/* Filtrando Valores Nulos */
SELECT NOME, SEXO, ENDERECO, EMAIL FROM CLIENTE WHERE EMAIL IS NULL;

/* Filtrando os Valores que não são Nulos */
SELECT NOME, SEXO, ENDERECO, EMAIL FROM CLIENTE WHERE EMAIL IS NOT NULL;